<%= form_with(model: relationship) do |form| %>
  <% if relationship.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(relationship.errors.count, "error") %> prohibited this relationship from being saved:</h2>

      <ul>
        <% relationship.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :member1_id, style: "display: block" %>
    <%= form.select :member1_id, Member.all.map {|e| [e.name, e.id]}  %>
  </div>

  <div>
    <%= form.label :member2_id, style: "display: block" %>
    <%= form.select :member2_id, Member.all.map {|e| [e.name, e.id]}  %>
  </div>

  <div>
    <%= form.label :types, style: "display: block" %>
    <%= form.select :types, ["parent", "spouse"].map {|e| [e, e]}  %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
